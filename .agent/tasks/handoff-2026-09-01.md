# Hand-off: what shipped on Sep 1, 2026 and what Tanav must do before Sep 3, 1:00pm PT

Everything below is on `main` and deployed at https://protocol-mirror.vercel.app. GitHub Actions is green for every commit listed.

## What shipped (commits a5f422a → 76b6d21)

| Commit | Change | Judge sees it in |
|---|---|---|
| a5f422a | Codex's 1080p60 slideshow render and media contract committed | video fallback |
| 3361c56 | `.agent/tasks/final-push-plan.md` (phases, owners, coordination rules) | repo |
| db4ea41 | `document.modelContext ?? navigator.modelContext`; registration errors logged; no tool count in preview mode | live URL |
| e754444 | **A real ClinicalTrials.gov/PubMed pair becomes the reviewable case.** Agent path: fetch both records → human clicks "Review this pair". Human path: curated chips (ACTT-1, Pfizer BNT162b2, RECOVERY dexamethasone) or an NCT + PMID form. Pair-bound tools re-register on the real ids; abstract sections labelled as such; receipt `generatedFrom: live_sources`; switch refused while decisions exist; enums dropped above 20 ids; executors accept object or JSON-string input. `scripts/webmcp-smoke.mjs` drives the full loop in headless Chrome 152 over CDP. | live URL, video, README |
| 0b49388 | Domain-first hero ("Did the trial publish what it registered?"), plain-word rail, CTA scrolls to the queue, "Uncertain" label, readable benchmark cards; `?fields=` projection so NCT04368728 returns 200 | live URL |
| eb3f520 | Session tool-call log + 7-chip roster with the receipt tool locked until a decision; provenance fix (human loader promotes the records it fetched); refusal notice next to the button; no-abstract guard; StrictMode-safe registration | live URL, video |
| bade0b2, 3bb5d69 | Screenshots regenerated from the real build; accessible names on outcome cards; contrast | README, Devpost |
| 9bb4f0d | XML entities decoded in abstract text (P<0.001); scroll margins under the sticky header | live URL |
| 76b6d21 | Locked chip contrast | live URL |

## Verification evidence

- `npm run check`: conformance PASS (7 tools, 3 registration effects), submission packet PASS, lint clean, 54 tests, production build.
- `npm run smoke:webmcp` against the local build and against https://protocol-mirror.vercel.app (Google Chrome 152.0.7977.65, `--enable-features=WebMCPTesting`): 6 tools → live reads (43 outcomes, 4 abstract sections) → "Review this pair" → `get_audit_state` reports the live case → `get_evidence_spans` returns real locators → `propose_outcome_mapping` stages → `request_human_review` focuses → DOM Accept → 7 tools → `export_review_receipt` reports `live_sources` → Undo → 6 tools. PASS.
- Lighthouse desktop on production: performance 100, accessibility 96 → contrast fix pushed in 76b6d21 (re-run after deploy), best practices 100, SEO 100.
- `curl https://protocol-mirror.vercel.app/api/clinical-trials/NCT04368728` → 200.
- Not yet verified: the new real-pair loop in the **Codex/ChatGPT in-app browser** (only the earlier flow was verified there on Aug 30–31). Do Step 1 below before recording.

## Step 1 — five-minute check in the Codex/ChatGPT in-app browser (do this first)

Open https://protocol-mirror.vercel.app in the in-app browser with site tools enabled. Header should read "WebMCP connected · 6 tools".

Paste these prompts one at a time:

1. `Call get_live_clinical_trial with nctId NCT04280705, then get_live_pubmed_article with pmid 32445440. Report only what the tools returned.`
   Expect: two intake cards (ACTT-1, 43 outcomes; NEJM abstract, 4 sections) and a "Review this pair" button.
2. Click **Review this pair** yourself. Expect: passport turns to "Live public record · active case", registry column shows "Time to Recovery", reported column shows abstract sections, header still 6 tools.
3. `Call get_audit_state, then get_evidence_spans for ev-registry-primary-1 and ev-publication-abstract-2. Quote both spans verbatim with their locators.`
   Expect: activeCase "live"; quotes "Time to Recovery" and the METHODS sentence; locators `protocolSection.outcomesModule.primaryOutcomes[0].measure` and `MedlineCitation.Article.Abstract.AbstractText[1]`.
4. `Stage one proposal: registryOutcomeId registry-primary-1, publicationOutcomeId publication-abstract-2, discrepancy uncertain, evidenceIds ev-registry-primary-1 and ev-publication-abstract-2, a two-sentence rationale, confidence 0.7. Then call request_human_review with the returned mappingId. Do not accept or reject.`
   Expect: the review queue scrolls into view with an UNCERTAIN card; the session log lists the calls.
5. `Accept that proposal for me.`
   Expect: the agent says it has no such tool. Click **Accept** yourself. Header flips to 7 tools; the roster unlocks `export_review_receipt`; "Download reviewed receipt JSON" appears.
6. `Call export_review_receipt and list its generatedFrom, locators, and events.`
   Expect: `live_sources`, both locators, `pair_loaded` / `mapping_staged` / `mapping_accepted`. Click **Undo last decision**; header returns to 6 tools.

If anything differs, tell Claude Code or Codex exactly which step and what you saw.

## Step 2 — record the video (target 2:15–2:30, own voice, under 3:00)

Screen-record the ChatGPT app with the in-app browser on the left two-thirds and the chat on the right third, address bar visible, notifications off. Hard cuts only while the model is thinking. Use the six prompts above in order; speak under 150 words total:

- 0:00 cold open on the connected page, Prompt 1 already typed: "No slides. This is ChatGPT working inside a real web page through WebMCP. Six tools, top right."
- Records land: "A trial registers what it promises to measure. Years later a paper reports what readers see. In a 2019 study of 389 trials, one in three changed a primary outcome, and those trials reported effects about 16 percent larger."
- Click Review this pair: "One click makes the real ACTT-1 record the case. The agent's tools rebind to its identifiers."
- Prompt 3: "Stable ids and exact quotations, flagged untrusted."
- Prompt 4: "Schema-bound proposal. The page validates it, stages it, and brings the checkpoint to me."
- Prompt 5: "I asked it to decide. It cannot. I read both spans and I click. Watch the top right: my decision just changed what the agent can do."
- Prompt 6 and Undo: "The receipt tool exists only while reviewed work exists. Undo, and it disappears."
- Ten seconds on the stress-test panel: "Across 24 real pairs, 48 of 48 live reads worked, and two small models failed in opposite directions. Same evidence, opposite errors. That is why the last call stays human."
- Close on the hero with the URL: "Protocol Mirror. Did the trial publish what it registered? A research-transparency aid, not medical advice."

Fallback if a clean recording is not done by Sep 3, 8:00am PT: upload the committed slideshow `docs/demo/protocol-mirror-submission-demo.mp4` (1080p60, 113 s, Kokoro narration) with the Variant B description in `docs/YOUTUBE_METADATA.md`.

## Step 3 — YouTube

Public, Science & Technology, English, no music. Title and description from `docs/YOUTUBE_METADATA.md`. Add chapters (first at 0:00, each ≥ 10 s). If you used the slideshow, answer the synthetic-voice disclosure truthfully. Watch the processed upload once at 1080p.

## Step 4 — Devpost form (submit by noon PT Sep 3)

Field values are prepared in `devpost-submission.md` (judge-facing text) and `docs/SUBMISSION_HANDOFF.md` (form-field table, ids 28249–28260). Your personal choices: submitter type, country, App Status (New: the repo was created 2026-08-29 during the challenge period), learning level, career value. Testing instructions field: the six steps above. Tested agents field: "Codex/ChatGPT desktop in-app browser (site tools enabled); Google Chrome 152 with chrome://flags/#enable-webmcp-testing" — only after Step 1 succeeded. Save the entry receipt.

## Optional if time remains (in priority order)

1. `gh repo edit TanavG223/protocol-mirror --description "Trial registry vs publication: agent cites, human decides (WebMCP)" --add-topic webmcp --add-topic clinical-trials --add-topic pubmed --add-topic human-in-the-loop --add-topic nextjs`
2. Move `docs/JUDGE_SCORECARD.md` (it compares named competitors) under `docs/internal/` and update the two scripts that reference it.
3. `load_trial_pair` as an eighth agent-callable tool (plan Phase 10) — only if Step 1 passed and a re-recording is not needed.

## Round two (Sep 2, ~00:30 ET) — commits 6921ea1, 173dfef

Shipped and verified (70 tests; `npm run smoke:webmcp` PASS locally and against production; history route live):

- **Eighth tool `get_registry_history`.** Reads the ClinicalTrials.gov registration versions (original + versions where Outcome Measures changed, capped) and reports primary-outcome changes with dates. ACTT-1: 25 versions; first registered 2020-02-20 with "Percentage of subjects reporting each severity rating on the 7-point ordinal scale (Day 15)"; changed v9 (2020-03-20) to an 8-point scale, v14 (2020-04-16) to "Time to recovery", v24 (2022-03-09) again. When a real pair is promoted, the ORIGINAL primary outcomes are listed first with `history/0…` locators so the agent can pair what was first registered against the publication. This is the product's core question answered for real.
- **Opens on a real trial.** First visit loads ACTT-1 with its history; `?demo` opens the fictional case; `?nct=&pmid=` deep-links a pair; "See 4 example proposals (demo case)" switches and loads the teaching proposals in one click.
- **Two-way channel.** Reject with a reason (picker + detail) and "Note to the agent"; both reach the agent via `get_audit_state` (`reviewerFeedback`, `reviewerNotes`).
- **Session persistence** in the tab (case, proposals, decisions, notes, log) with "Clear session"; word-level diff of short paired quotes in the evidence drawer.
- Header counts are now 7 tools before a decision, 8 after.

Updated judge path (use these prompts in the in-app browser; the page already opens on ACTT-1):
1. `Call get_audit_state. Tell me the registry history summary and list the registry outcome ids whose id starts with registry-original.`
2. `Call get_evidence_spans for ev-registry-original-primary-1 and ev-publication-abstract-3 and quote both spans with their locators.`
3. `Stage one proposal: registryOutcomeId registry-original-primary-1, publicationOutcomeId publication-abstract-3, discrepancy uncertain, both evidence ids, a two-sentence rationale, confidence 0.7. Then call request_human_review with the returned mappingId. Do not accept or reject.`
4. Click **Reject**, pick a reason, confirm. Then: `Call get_audit_state and read reviewerFeedback. Revise your proposal accordingly and stage it again.` (the agent reads your reason and re-proposes)
5. Click **Accept** on the new proposal. Header → 8 tools. `Call export_review_receipt and list generatedFrom, locators and events.` Expect `live_sources` and a `history/0…` locator.
6. Click **Undo last decision** → 7 tools. Reload the page: the case and proposals are still there.

Still pending for Tanav: in-app browser check of this new loop (Step 1 above), the recording, YouTube, Devpost.

## Round three (Sep 2, ~01:40 ET) — commit d77e447

Blind critic round three found five blockers; all fixed and covered by tests (70 passing), local smoke PASS:

- **Pfizer curated pair no longer 502s.** One registration version of NCT04368728 is 4.2 MB; history versions now allow 8 MB and an unreadable version is reported in `unreadVersions`, never fatal.
- **Honest sampling.** Six or fewer Outcome Measures versions → all compared (`complete: true`). More → original + newest + a bisection that dates the first change; each change carries `exact` and `after` (the newest compared version that still had the previous set). The strip and the amber note say "N of M versions compared" and "between vA and vB" where a date is not exact. Pfizer: 5 of 53 compared, first change exact at v6 (2020-07-15).
- **Zero original primaries** → no empty quotes; the note says primary outcomes first appear in vN.
- **History text entity-decoded** (RECOVERY's `&#x27;no additional treatment&#x27;`).
- **Opening-load race closed**: hero button disabled while loading; an automatic load never paints an alert.
- Also: `get_audit_state` accepts `outcomes: "primary" | "all"` (primary is the default for live records with >20 outcomes; the response says how many were omitted); PubMed publication date (electronic first) shown as "Published …" and used for "2 of 3 changes predate the publication (2020-10-08)" on ACTT-1; a proposal staged while the reviewer is mid-rejection waits instead of closing the form; `?nct=` without a PMID prefills the form; evidence-drawer identity stacks vertically; roster says `request_human_review` "focuses only".

Verified numbers for the video/README: ACTT-1 has 25 registration versions, 4 Outcome Measures versions, all compared; the primary outcome set changed in v9 (2020-03-20, 8-point scale), v14 (2020-04-16, time to recovery) and v24 (2022-03-09, adds subgroup entries); the PubMed record's electronic date is 2020-10-08 (the final report), so two of three changes predate it.

## Round four (Sep 2, ~00:40 ET)

Blind critic round four (ran the README judge path on the round-three build: worked end to end, zero console errors) found two blockers, both fixed, plus small items:

- **Measures only.** A time-frame-only registry edit was counted as a primary-outcome change, so a benchmark "no change" trial (NCT00347321) showed a change with identical text on both sides. The comparison is now measure-only and order-insensitive; time-frame edits between specified time frames are reported separately as `timeFrameEdits` and shown as "Time frames were edited without changing the measures in vN". NCT00347321 → no change; Moderna COVE NCT04470427 → 2 changes + 2 time-frame edits (was 4 changes). Curated pairs unchanged (ACTT-1 v9/v14/v24).
- **Long notes cut.** Change lists show three measures then "and N more" (Pfizer's note was 16,000 characters). Full lists still reach the agent through `registryHistory.changes`.
- Also: a re-decided mapping no longer keeps the previous rejection reason (receipt was self-contradictory); "changes predate the publication" is hedged when an inexact window straddles the date ("At least 2 of 4 …; 1 more falls in a window that straddles it"); registry column says "43 outcomes + 1 from the original registration" and `get_audit_state.registryOutcomeCount` is the current record's count; the hero button's case switch surfaces the discard notice; a malformed saved session is discarded instead of white-screening, and `src/app/error.tsx` offers "Clear the saved session and reload"; aborted registrations on unmount no longer log errors; `?pmid=` alone prefills the form; the module-label guard marks the result incomplete instead of a false "unchanged"; `outcomes: "bogus"` is rejected. 75 tests. README/devpost now name all three ACTT-1 changes and the real fetch cap (nine versions).

## Sep 2, ~01:30 ET — in-app browser verified; hydration slip fixed

- **Codex ran the new loop in the ChatGPT desktop in-app browser (site tools enabled) and reported it working end to end**: ACTT-1 loads automatically; 25 versions with the three dated changes; 7 tools → 8 after a human decision; rejection feedback reaches the agent; the agent revises and re-proposes; the receipt carries live-source provenance and a `history/0…` locator; reload persistence works. This is the evidence for the Devpost "tested agents" field.
- Codex also saw React error 418 on production at `7253b83`. Cause: the origin-trial slot rendered an empty string that hydrated differently. Fixed in `62effa1`; headless Chrome reports zero console errors on `/` and `/?demo` in production. `scripts/webmcp-smoke.mjs` now fails on any uncaught page error or `console.error` (`9cd854d`).
- Undo semantics for the video: after reject → revise → accept there are two reviewed decisions, so it takes two Undo clicks to return to 7 tools. Expected.
- Codex is remaking the video around the live ACTT-1 flow; the old 6→7 slideshow will not be reused.

## Sep 2, ~00:30–01:00 ET — UI polish (commits eed1d5d, 24b0266)

Tanav asked for a more reactive, less "vibe coded" UI and shared a screenshot at ~960 px showing the hero button clipped and the trial title wrapping one word per line. Three-lens design critique (layout, motion, depth) plus a distillation of two reference repos (ui-ux-pro-max-skill, react-components-for-claude-code) fed two passes:

- **Layout.** Hero action stack wraps under 1180 px (no clipping); case card columns give the title the widest track; source strip 2×2 under 1180; identifiers wrap; reject form fields shrink (no sideways scroll on phones); classification chips get a max-content track; evidence quotes stack under 1180; mobile headline clamp; hero panel bound to hero content. No horizontal overflow from 390 to 1280 px (measured).
- **Motion.** Tokens (`--dur-fast/base/slow`, `--ease-out/in-out`); entrance in CSS from first paint (GSAP removed); staggered review-card mount; sheen only on newly staged cards; badge ring + rolling count only when the tool count flips 7↔8; receipt chip inks amber→green on unlock; evidence drawer and decision notice animate on change; pointer spotlight on review cards (rAF-coalesced); scroll-driven reveals under `@supports (animation-timeline: view())`; reduced-motion block also zeroes delays.
- **Depth/contrast.** Layered ambient shadow tokens, dark tile highlight, warm bloom on the amber note, contrast fixes (classification chips, disabled buttons, dots, review dock), SVG lock instead of emoji.
- **New "Ask your agent" panel** under the hero: three copyable prompts built from the case on the page (the ids adapt to the loaded pair).
- Verified: 75 tests, conformance + packet, smoke locally, zero console errors, Lighthouse 100/100/100/100 on production after the first pass; second pass verification recorded below when complete.

Final verification, 2026-09-02 ~01:15 ET: commit `74622c3` deployed; production smoke PASS; Lighthouse 100/100/100/100; zero console errors on two fresh loads; CI green. UI frozen; Codex cleared to record.

## Sep 2, ~01:55 ET — Vercel Security Checkpoint

Production answers HTTP 403 "Vercel Security Checkpoint" (`x-vercel-mitigated: challenge`) to curl, WebFetch and headless Chrome since ~00:50 ET; a real browser (desktop app browser pane) passes it in a few seconds and reaches the app. Likely cause: Vercel's automatic mitigation after hours of automated verification traffic from one address (smoke runs, Lighthouse, deploy polling). All automated production checks are stopped. **Tanav: open the Vercel dashboard → Protocol Mirror → Firewall. If "Attack Challenge Mode" is enabled, turn it off before judging starts (Sep 4); if it is automatic mitigation it should lift on its own — re-check in a few hours.** Because of this, the depth pass `be9210b` has NOT been smoke-tested on production; the last production-verified application commit is `74622c3`, and `be9210b` changed only CSS (verified locally: 75 tests, smoke PASS, zero console errors, Lighthouse accessibility 100).

## Sep 2, ~08:40 ET — final video committed (ea72605)

Tanav approved Codex's re-rendered master (`docs/demo/protocol-mirror-submission-demo.mp4`: 127.90 s, 1080p60, AAC −16.4 LUFS, 24 caption cues, media contract PASS; frames show the current product and the narration states 7→8 tools and the ACTT-1 dates). Committed with Git LFS because the file is 116 MB (> GitHub's 100 MB limit). **Remaining for Tanav:** (1) upload exactly this file to YouTube, public, using the title, description and chapters in `docs/YOUTUBE_METADATA.md`, answer the synthetic-voice disclosure truthfully (Kokoro-82M narration), watch the processed upload once; (2) paste the URL to Claude so it goes into `docs/SUBMISSION_HANDOFF.md` and the Devpost text; (3) fill the Devpost form from `docs/SUBMISSION_HANDOFF.md` and submit before Sep 3, 1:00pm PT; (4) check the Vercel Firewall for Attack Challenge Mode before judging starts.

## Sep 2, ~09:20 ET — origin trial live

Tanav registered the Chrome WebMCP origin trial; token served from commit `d8de2a8`. Verified: Chrome 152 with no flag on the live origin → `document.modelContext` present, badge "WebMCP connected · 7 tools". Judge-facing copy now says no flag is needed on the live URL. Vercel checkpoint has lifted (200). Production smoke on `d8de2a8`: PASS.

## Sep 2, ~09:30 ET — fresh-clone judge test PASS

A clean clone of the public repo (Git LFS disabled, pointer files in place) followed the README only: `npm install` (392 packages, 0 vulnerabilities), `npm run check` (75/75, build), `npm run build`, `npm start -- -p 4190`, both API curls 200/ok, and the repo's browser smoke `WEBMCP_SMOKE=PASS`, in 3m19s. README gaps found by the run and a newcomer audit were fixed the same hour: production build/start commands, Chrome flag for a local origin, which agent to paste into, smoke prerequisites (running server, Chrome path, `--port`, network), `.nvmrc`, Git LFS note, neutral wording in the runbook. Sandbox and scratch artifacts deleted afterwards.
